<!DOCTYPE html>
<html>
<head>
    <title>Happy Birthday Mom</title>
    <style>
        body {
            background: #e0f2fe;
            font-family: Arial, sans-serif;
            overflow: hidden;
            margin: 0;
            height: 100vh;
            position: relative;
        }

        /* Center the message */
        .center-message {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #1e3a8a;
            font-size: 48px;
            text-align: center;
            animation: glow 1.5s infinite alternate;
            z-index: 100;
        }

        @keyframes glow {
            0% { text-shadow: 0 0 5px #93c5fd, 0 0 10px #60a5fa; }
            50% { text-shadow: 0 0 15px #3b82f6, 0 0 25px #60a5fa; }
            100% { text-shadow: 0 0 5px #93c5fd, 0 0 10px #60a5fa; }
        }

        p { 
            color: #1e40af; 
            font-size: 18px; 
            margin: 20px; 
            line-height: 1.6; 
            text-align: center;
            position: absolute;
            top: 65%;
            left: 50%;
            transform: translateX(-50%);
            z-index: 100;
        }

        /* Hearts */
        .heart { 
            position: absolute; 
            font-size: 22px; 
            animation: floatUp 6s infinite; 
            cursor: pointer; 
            transition: transform 0.3s; 
        }

        .heart.pop { 
            transform: scale(2) rotate(360deg); 
            color: #f43f5e; 
        }

        @keyframes floatUp { 
            from { bottom: 0; opacity: 1; } 
            to { bottom: 100%; opacity: 0; } 
        }

        /* Balloons */
        .balloon { 
            position: absolute; 
            bottom: -100px; 
            font-size: 50px; 
            animation: balloonUp 10s linear infinite; 
            cursor: pointer; 
            transition: transform 0.3s; 
        }

        .balloon.clicked { 
            transform: scale(1.5) rotate(-20deg); 
        }

        @keyframes balloonUp { 
            from { bottom: -100px; } 
            to { bottom: 110%; } 
        }

        /* Cats */
        .cat { 
            position: absolute; 
            bottom: 10px; 
            font-size: 45px; 
            animation: walk 12s linear infinite; 
        }

        @keyframes walk { 
            from { left: -100px; } 
            to { left: 100%; } 
        }

        /* Popup */
        #popup {
            position: fixed; 
            top: 50%; 
            left: 50%; 
            transform: translate(-50%, -50%);
            background: #fff; 
            padding: 30px; 
            border-radius: 20px; 
            box-shadow: 0 0 20px #60a5fa;
            font-size: 22px; 
            color: #1e40af; 
            display: none; 
            z-index: 1000; 
            max-width: 80%;
            text-align: center;
        }

        #popup button { 
            margin-top: 20px; 
            padding: 10px 20px; 
            background: #3b82f6; 
            color: #fff; 
            border: none; 
            border-radius: 10px; 
            cursor: pointer; 
        }

    </style>
</head>
<body>

    <!-- Centered Happy Birthday Message -->
    <div class="center-message">🎉 Happy Birthday Mom 🎉</div>

    <!-- Personal message -->
    <p>
        Dear Mom, you are my strength, my comfort, and my greatest blessing. 
        I love you endlessly, today and always. 💖
    </p>

    <!-- Hearts -->
    <div class="heart" style="left:15%;">💙</div>
    <div class="heart" style="left:35%;">💖</div>
    <div class="heart" style="left:55%;">💙</div>
    <div class="heart" style="left:75%;">💖</div>

    <!-- Balloons with messages -->
    <div class="balloon" style="left:10%;" data-message="💖 Mom, you are my world! 💖">🎈</div>
    <div class="balloon" style="left:30%;" data-message="🌸 You make life beautiful 🌸">🎈</div>
    <div class="balloon" style="left:50%;" data-message="🎉 Happy Birthday Mom! 🎉">🎈</div>
    <div class="balloon" style="left:70%;" data-message="❤️ Thank you for always loving me ❤️">🎈</div>
    <div class="balloon" style="left:90%;" data-message="💫 You are my inspiration 💫">🎈</div>

    <!-- Cats -->
    <div class="cat" style="animation-delay:0s;">🐱</div>
    <div class="cat" style="animation-delay:3s;">🐈</div>
    <div class="cat" style="animation-delay:6s;">😺</div>

    <!-- Popup -->
    <div id="popup">
        <span id="popupMessage"></span><br>
        <button onclick="closePopup()">Close</button>
    </div>

    <script>
        // Balloons click popup
        const balloons = document.querySelectorAll('.balloon');
        const popup = document.getElementById('popup');
        const popupMessage = document.getElementById('popupMessage');

        balloons.forEach(balloon => {
            balloon.addEventListener('click', () => {
                balloon.classList.add('clicked');
                popupMessage.textContent = balloon.getAttribute('data-message');
                popup.style.display = 'block';
            });
        });

        function closePopup() { popup.style.display = 'none'; }

        // Heart pop
        const hearts = document.querySelectorAll('.heart');
        hearts.forEach(heart => {
            heart.addEventListener('click', () => {
                heart.classList.add('pop');
                setTimeout(() => { heart.classList.remove('pop'); }, 500);
            });
        });
    </script>

</body>
</html>
